<Window x:Class="RSAddin.UI.Views.BeamCreatorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RSAddin.UI.ViewModels"
        mc:Ignorable="d"
        Title="Beam Creator" Height="550" Width="400">
    <Window.DataContext>
        <local:BeamCreatorViewModel/>
    </Window.DataContext>
    <StackPanel Margin="10">
        <TextBlock Text="Parámetros de la Viga" FontWeight="Bold" Margin="0,0,0,10"/>

        <TextBlock Text="Nodo Inicial (X, Y, Z):"/>
        <StackPanel Orientation="Horizontal">
            <TextBox Text="{Binding BeamParams.StartNode.X, UpdateSourceTrigger=PropertyChanged}" Width="50" Margin="5"/>
            <TextBox Text="{Binding BeamParams.StartNode.Y, UpdateSourceTrigger=PropertyChanged}" Width="50" Margin="5"/>
            <TextBox Text="{Binding BeamParams.StartNode.Z, UpdateSourceTrigger=PropertyChanged}" Width="50" Margin="5"/>
        </StackPanel>

        <TextBlock Text="Nodo Final (X, Y, Z):"/>
        <StackPanel Orientation="Horizontal">
            <TextBox Text="{Binding BeamParams.EndNode.X, UpdateSourceTrigger=PropertyChanged}" Width="50" Margin="5"/>
            <TextBox Text="{Binding BeamParams.EndNode.Y, UpdateSourceTrigger=PropertyChanged}" Width="50" Margin="5"/>
            <TextBox Text="{Binding BeamParams.EndNode.Z, UpdateSourceTrigger=PropertyChanged}" Width="50" Margin="5"/>
        </StackPanel>

        <TextBlock Text="Nombre de Sección (ej: IPE 200):"/>
        <TextBox Text="{Binding BeamParams.SectionName, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>

        <TextBlock Text="Apoyo en Nodo Inicial:"/>
        <CheckBox IsChecked="{Binding BeamParams.SupportStartNodeFixed}" Content="Fijo (empotrado)" Margin="5"/>

        <TextBlock Text="Apoyo en Nodo Final:"/>
        <CheckBox IsChecked="{Binding BeamParams.SupportEndNodeFixed}" Content="Fijo (empotrado)" Margin="5"/>
        <TextBlock Text="(Si no está marcado, será un apoyo simple restringiendo UX, UZ)" Margin="5,0,5,5" FontStyle="Italic" FontSize="10"/>


        <TextBlock Text="Valor Carga Puntual en Centro (N, negativo hacia abajo):"/>
        <TextBox Text="{Binding BeamParams.PointLoadValue, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>

        <TextBlock Text="Caso de Carga para Carga Puntual:"/>
        <ComboBox ItemsSource="{Binding AvailableLoadCaseTypes}"
                  SelectedItem="{Binding BeamParams.LoadCaseForPointLoad}" Margin="5"/>

        <Button Content="Crear Modelo en Robot" Command="{Binding CreateModelCommand}" Margin="0,20,0,5"/>
        <Button Content="Ejecutar Análisis" Command="{Binding RunAnalysisCommand}" Margin="0,5,0,5"/>
        <Button Content="Obtener Deflexión (Barra 1, Caso Actual)" Command="{Binding GetDeflectionCommand}" Margin="0,5,0,20"/>

        <TextBlock Text="Estado/Resultados:" FontWeight="Bold"/>
        <TextBox Text="{Binding StatusMessage, Mode=OneWay}" MinHeight="60" TextWrapping="Wrap" IsReadOnly="True" VerticalScrollBarVisibility="Auto"/>
    </StackPanel>
</Window>
