# DeepSeek API Configuration
DEEPSEEK_API_KEY="TU_DEEPSEEK_API_KEY_AQUI"

# Dlubal RSTAB Configuration
# Ejemplo para conexión a WebService (ajustar según sea necesario)
RSTAB_API_ENDPOINT="http://localhost:8081" # O la URL del servidor RSTAB
RSTAB_USERNAME="TU_USUARIO_RSTAB" # Si es necesario
RSTAB_PASSWORD="TU_CONTRASENA_RSTAB" # Si es necesario

# Ejemplo para identificar un archivo local (si se usa COM o para abrir archivos específicos)
# RSTAB_MODEL_FILE_PATH="C:/ruta/a/tu/modelo.rs9" # O .rs8, .glb, .igs, etc.

# Configuración regional o de normativas
DEFAULT_REGION_CODE="MX" # Ejemplo para México
